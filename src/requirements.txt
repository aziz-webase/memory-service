# API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Postgres + pgvector (turns, memories, embeddings, vector search)
psycopg[binary,pool]>=3.1.0
pgvector>=0.2.5

# OpenAI (extraction chat + embeddings)
openai>=1.30.0

# Cross-encoder reranker (BAAI/bge-reranker-v2-m3)
sentence-transformers>=3.0.0
torch>=2.1.0
transformers>=4.40.0
